import { RouteLoading } from "../components/RouteLoading";
export default function Loading() { return <RouteLoading eyebrow="Public data operations" title="Reading the local trial index" description="Loading source freshness and ingestion receipts without patient data." status="Preparing the data health view" steps={[{ label: "Index status", detail: "Read the configured SQLite or PostgreSQL public index." }, { label: "Source receipts", detail: "Load TFDA and ClinicalTrials.gov synchronization metadata." }, { label: "Integration gates", detail: "Show active, optional, and rights-review sources separately." }]} />; }
