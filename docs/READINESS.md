# Readiness status

## Verified in this repository

- Clean-room replacement and tracked-file legacy scan.
- Anonymous in-memory chat state with no browser persistence API.
- Browser masking fixtures plus server-side identifier rejection.
- Exact `gpt-oss:120b-cloud` allowlist, loopback-only proxy, and prohibition on local GPU or CPU inference.
- Patient-confirmed profile boundary and separate cloud/WebMCP consent.
- Live TFDA zipped JSON parsing plus a production-shaped scheduled snapshot command and reader. The schema-1.0 artifact is fixed-source, size/count/SHA-256 validated, atomically replaced with a bounded sidecar, fresh for 24 hours, stale through seven days, and then rejected. Visible, WebMCP, and health receipts distinguish request-time process memory from a scheduled file without exposing its path or records.
- Recorded live TFDA ingestion evidence on 2026-09-02: 18,493 official public records produced a 175,674,023-byte artifact with SHA-256 `88bfe2a4e6e8258e9371fc722e2e180666b3ebcb0f5d12451675493ea2c9b0d1`; the scheduled-file adapter returned three bounded `胃癌` results while a throwing fetcher proved no network fallback. Only the metadata receipt remains in Git; the temporary large snapshot was removed.
- Independent 20-second registry deadlines, upstream abort propagation for ClinicalTrials.gov, machine-readable timeout/unavailable codes, per-source latency receipts, and partial-result preservation when another source succeeds.
- End-to-end imperative WebMCP cancellation from execution callback through browser fetch, Next.js request, matching, and both registry adapters, with exact abort-reason preservation and shared TFDA snapshot isolation covered by deterministic tests.
- A visible, versioned bilingual query plan covering all 19 declared cancer groups, with exact-term mapping and non-inferential pass-through for unrecognized detail.
- Taiwan-to-Asia-to-world ranking, explicit-ID deduplication, and source traces.
- A deterministic four-domain detailed-criteria map for histologic subtype, stage, biomarker, and prior treatment. Each signal keeps the confirmed patient fact, public registry field and excerpt, and an explicit shared-term / possible-difference / uncertain / missing label. These wording signals do not change overall match status; the separately traced prior-treatment exclusion signal retains its existing review effect.
- Live synthetic `gpt-oss:120b-cloud` extraction, confirmed-profile matching, and separately consented cloud dialogue.
- A compact volatile extraction receipt that shows requested/provider-reported model labels, localhost-proxy/remote-cloud transport, actual server latency, TrialBridge non-persistence, and a bounded recovery code after failure without copying medical or model content.
- One visible declarative WebMCP registry-search form plus up to seven read-only imperative tools, output limits, untrusted-content annotations, consent-driven sensitive registration, state-aware pending-question recovery, and a comparison capability that appears only after two visible shortlist selections.
- One canonical source for all eight tool contracts. The runtime imperative tools, visible declarative form, searchable `/webmcp` Tool Contract Explorer, deterministic verifier, and static `/webmcp/contracts.json` artifact share the same names, descriptions, schemas, and annotations. All eight contracts remain read-only in behavior, expose zero write authority, and meet the audited name/description/parameter/output budgets.
- A responsive no-PHI Capability State Simulator that exposes the human-controlled `2 → 2 → 6 → 7` imperative registration sequence. Tests compare all four synthetic states against the real runtime builder: confirming a summary alone adds nothing, visible permission adds four contextual tools, and a second visible shortlist selection adds only the comparison tool. The simulator executes no tool and does not claim current-browser evidence.
- Eleven deterministic WebMCP journey cases plus a version-locked, no-PHI `gpt-oss:120b-cloud` selection baseline: 55/55 expected tool calls or safe abstentions across direct, ambiguous, recovery, and forbidden intents in one uninterrupted run. Shortlist comparison was 5/5 and forbidden abstention was 10/10. This does not replace the browser Inspector gate.
- Deterministic WebMCP conformance verification and GitHub CI gates for tests, types, clean-room boundaries, dependency audit, and production build.
- A built-in `/webmcp` competition-evidence page that reports current-browser support, public tool discovery, same-origin security headers, safe method execution, and the remaining Inspector boundary.
- A repository-verified `/webmcp/quickstart` three-minute entrance that exposes the same two public definitions, origin-scoped discovery, independent header status, and only one explicit fixed no-input method execution. It uses no free text, cloud model, registry search, patient context, workflow mutation, or persistence; live current-browser and Inspector acceptance are not inferred from repository checks.
- Root `/llms.txt`, a canonical-catalog-derived `/webmcp/agent-guide.md`, and `rel="describedby"` discovery. They give agents explicit public/permission/shortlist boundaries and exact evidence links while remaining no-input, no-PHI documentation separate from the WebMCP runtime.
- Recorded Lighthouse 13.4.1 Agentic Browsing evidence for two page states: quickstart exposed both public imperative tools, trials exposed the declarative form tool, and both passed schema validity, accessibility-tree integrity, `llms.txt`, and CLS 0. The metadata-only artifact excludes raw reports, page text, arguments, and outputs; it does not replace natural-language Inspector or production Origin Trial acceptance.
- A three-layer browser setup surface and canonical machine-readable setup contract: WebMCP itself requires no visitor extension, Chrome local testing uses one copyable native flag, TrialBridge registers tools automatically, and Model Context Tool Inspector stays a separate optional judge utility. The contract reads no browser or medical-workflow state and is included in `/webmcp/evidence.json`.
- An explicit six-check no-PHI runtime lifecycle suite that temporarily registers one fixed read-only/no-network probe, verifies discovery and contract metadata, executes the bounded public method tool, checks execution AbortSignal propagation plus `toolchange`, aborts registration, and requires post-run cleanup. Its schema-1.1 download stores outcomes only; deterministic mocks cover draft object input, current-Chrome serialized input, failure reporting, and cleanup.
- Recorded live-browser evidence on 2026-09-02: an isolated Chrome for Testing Beta 153.0.8010.12 local-testing profile exposed `document.modelContext`, discovered both public tools, and passed all 6/6 lifecycle checks with zero console errors. Chrome returned serialized discovery schemas and asynchronous cancellation propagation; both are now regression-tested. The temporary probe was absent afterward and only the two public tools remained. `evals/webmcp-browser-runtime-acceptance.json` stores metadata only and passes the offline receipt verifier.
- Recorded stock-Inspector evidence on 2026-09-02: the unmodified upstream extension at commit `f164a9aa5c3f6083f5976ccae308257bdf86cb99`, version 1.9.14, ran in the isolated Chrome for Testing profile. Its real background/content-script/sidebar path discovered both public tools, parsed both schemas, manually invoked the fixed read-only method tool, and observed the fictional permission lifecycle as `2 → 2 → 6 → 2`: 3/6 checks passed. Pinned-source audit found no stock agent cancel control or execution `AbortSignal`; TrialBridge's separate visible human cancellation reaches the active request but is not counted as an Inspector pass. Three checks remain not run; no Gemini key/call, health information, raw schema, or execution result is retained in `evals/webmcp-inspector-extension-runtime.json`.
- A fail-closed first-party Origin Trial deployment contract: the default state is `local_testing_only`; a token is accepted only with an exact non-loopback HTTPS `SITE_URL`, emitted through server-rendered meta before WebMCP access, excluded from client JavaScript and JSON, and reported only as `configured_unverified` until Chrome DevTools and Inspector acceptance. Invalid token/origin shape degrades health and fails the build.
- A five-stage critical-user-journey contract that maps goal, initial state, available tools, visible page reactions, and recovery, plus a filtered download-only browser diagnostic receipt with no health information or tool payloads.
- A six-check manual Chrome Inspector acceptance kit covering discovery/schema parsing, safe invocation, bilingual selection, forbidden abstention, permission transitions, and cancellation/cleanup. Its volatile outcomes and download-only receipt exclude prompts and payloads, remain explicitly self-attested, and have a deterministic offline verifier.
- An explicit no-input cloud availability probe for judges: fixed synthetic prompt, exact cloud-model allowlist, 30-second hard limit, cancel/retry states, three checks per 10 minutes, and metadata-only output/receipt with no model content.
- A body-free, one-click competition preflight that checks the fixed cloud probe and both public registries in parallel, preserves source-level ready/partial/unavailable states, shares the cloud-probe rate limit, and returns no trial records, model content, or health information.
- A four-scenario live Agent Rehearsal for competition review. Only fixed synthetic journey IDs are accepted; one Traditional Chinese search, one public-method request, one user-controlled shortlist comparison, and one forbidden enrollment request exercise live `gpt-oss:120b-cloud` tool selection. The route is bounded to 256 request characters, shares the three-per-ten-minute cloud allowance, propagates cancellation, redacts invented capability names, returns no model prose or thinking, executes no tool, changes no workflow state, and persists nothing. This is live model-selection evidence, not Chrome Inspector or clinical evidence.
- A separate fixed public browser-execution proof appears only after the Traditional Chinese rehearsal selects the expected search tool. One explicit action discovers the current-origin capability and executes exactly `胃癌` through `document.modelContext.executeTool()`, with read-only/untrusted-content enforcement, object/serialized compatibility, cancellation, a 25-second stop, and a bounded volatile receipt. It is site-orchestrated evidence and is never presented as Inspector or external-agent initiation.
- Live Agent Rehearsal evidence on 2026-09-02: the fixed Traditional Chinese search selected `search_public_cancer_trials` in 1,343 ms; requested `gpt-oss:120b-cloud`, provider-reported `gpt-oss:120b`, valid synthetic arguments, no execution, no health information, no model-content storage, and no persistence. The adjacent production Lighthouse audit recorded Performance 92, Accessibility 100, Best Practices 100, CLS 0, zero console errors, and zero non-composited-animation findings.
- Historical live competition-preflight evidence on 2026-09-02: overall ready in 5,528 ms; `gpt-oss:120b-cloud` provider response in 647 ms, TFDA live response in 5,524 ms, and ClinicalTrials.gov live response in 394 ms. The earlier Recruiting-only query returned zero TFDA records because the current TFDA export does not publish a dependable recruitment-status field. The current preflight and matching search include status-unpublished records, label them unknown, and never fabricate Recruiting.
- Live no-PHI probe evidence on 2026-09-02: requested `gpt-oss:120b-cloud`, provider reported `gpt-oss:120b`, loopback proxy plus remote-cloud inference, 1,202 ms, with the request-body rejection boundary separately exercised as HTTP 400.
- A judge-visible, dated WebMCP standards profile plus tested compatibility for the upstream object-input/current-Chrome serialized-input execution boundary and the upstream/current-Chromium cancellation event names.
- An eight-clause specification crosswalk pinned to upstream commit `41d12f057167ccf5954dbcf49d99502cb6c84491`: seven implemented imperative/lifecycle/security rows and one deliberately `explainer_aligned` declarative row. CI rejects missing repository evidence paths, and the bundle explicitly sets `claimedNormativeDeclarativeConformance` to false while the upstream declarative section remains TODO.
- A dated, primary-source-linked implementation landscape for ChatGPT Desktop, Chrome 149, and Brave Leo, explicitly bounded as source-reported ecosystem status rather than local runtime verification.
- A ten-row judge conformance matrix and static `/webmcp/evidence.json` bundle that separate seven repository-verified claims, one recorded stock-Inspector browser result, one recorded model evaluation, and one remaining manual Inspector gate. The bundle carries source paths, links, exact artifact metadata, and both recorded browser summaries while reading no browser-session or medical-workflow state.
- A responsive four-step judge runbook plus shareable broad-cancer search links that reopen the same visible declarative form. A fixed no-PHI synthetic-case deep link server-renders at the privacy boundary without skipping protected stages and removes itself when the anonymous conversation is cleared. URL state rejects direct identifiers, multiline content, and unrecognized detailed conditions; production Lighthouse verified default mobile, 375×667, and 844×390 landscape layouts.
- Shared route-level loading UI now gives `/match`, `/trials`, and the `/webmcp` route family immediate destination-specific feedback through native Next.js `loading.tsx` boundaries. It uses one reusable server component, a polite atomic status, stable reserved layout, an explicit home escape, no fake completion estimate, and a reduced-motion fallback.
- A bounded, payload-free WebMCP session receipt that makes capability registration changes and tool lifecycle states visible and optionally downloadable without server persistence.
- TypeScript, production build, dependency audit, HTTP routes, and security headers.
- A payload-free configuration health endpoint that degrades for a configured missing, invalid, or expired TFDA snapshot, plus pre-body process-local rate limits with machine-readable `429`/`Retry-After` responses.
- Fail-closed public discovery: canonical and share metadata, local-font Open Graph/Twitter images, a manifest, and dynamic robots/sitemap routes. Indexing requires an explicit non-loopback HTTPS deployment profile and remains disabled by default.

## Not yet proven and required before public clinical use

- No cancer group is clinically validated. All 19 coverage groups are `unreviewed`; searchability is not accuracy.
- The 19-group bilingual query lexicon is an engineering navigation aid and has not been adjudicated by an oncology terminologist; its visible mapping is not a clinical translation claim.
- Deterministic masking cannot guarantee removal of every name or contextual identifier.
- Provider retention, data-processing location, terms, outage behavior, and production latency for cloud extraction are not yet accepted.
- The scheduled TFDA ingestion command and fail-closed reader are implemented, but no actual production scheduler, shared durable volume, backup/restore policy, stale/expiry alert, operator ownership, or recovery drill has been provisioned and verified. Without `TFDA_SNAPSHOT_PATH`, development still uses request-time cold loading by design.
- Detailed eligibility wording beyond subtype, stage, biomarker, and prior treatment is not atomically assessed. The four implemented domains are conservative term-level navigation signals, not protocol adjudication or final eligibility.
- No oncologist/research-nurse adjudicated gold set, false-eligible measurement, calibration, or subgroup fairness evaluation exists.
- Taiwan privacy-law review, clinical governance, threat-model review, incident response, data-processing records, abuse controls, and user research are incomplete.
- Stock Model Context Tool Inspector acceptance is partial at 3/6. Its background/content-script/sidebar path proves public discovery/schema parsing, fixed safe manual execution, and the fictional local `2 → 2 → 6 → 2` permission lifecycle. Traditional Chinese natural-language selection, forbidden-request abstention, stock agent cancellation/cleanup, a user-opened side-panel surface, and production Origin Trial behavior remain unproven. The natural-language path was not run because the stock extension requires Gemini while TrialBridge permits only `gpt-oss:120b-cloud`; pinned source also shows that stock 1.9.14 offers no cancel control and passes no execution signal. TrialBridge now provides a separately tested, human-visible stop for active tools and cleans unfinished work on navigation, without upgrading the stock score. A production Origin Trial token has also not been supplied or validated in Chrome; the separate 55-sample Ollama selection baseline does not replace these gates.
- The new model-selection-to-fixed-public-execution sequence is repository-tested and its selection plus registry dependencies were separately live-checked, but the exact combined second-click `executeTool()` path has not yet been recorded in a compatible Chrome session. The interface and evidence bundle label it site-orchestrated and do not infer a browser pass from HTTP or unit tests.
- Manual large-text, keyboard-only, and screen-reader acceptance has not been run in a real browser. Automated production Lighthouse now covers 375×667 and 844×390 landscape with Accessibility 100, zero console errors, and no non-composited animation finding; this is not equivalent to assistive-technology testing.

TrialBridge TW is therefore an engineering MVP for controlled local development, not a public clinical service.
